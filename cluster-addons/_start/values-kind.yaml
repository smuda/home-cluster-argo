kubecost:
  use: false

metricsServer:
  use: true

prometheus:
  use: true
  podSecurityLevel: privileged

pvProviders:
  nfs:
    use: false
  csiDriverNFS:
    use: false

sealedSecrets:
  namespace: addon-sealed-secrets
