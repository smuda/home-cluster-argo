quota:
  use: true
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.memory: 1Gi

upstream:
  isOpenshift: true
  reloader:
    serviceMonitor:
      enabled: true
    deployment:
      securityContext: false
