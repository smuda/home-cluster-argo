{{ if .Values.certManager.use }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dexidp-tls
spec:
  secretName: dexidp-tls
  dnsNames:
    - {{ .Values.hostname }}
  issuerRef:
    name: dex-issuer
    kind: Issuer
{{ end }}
